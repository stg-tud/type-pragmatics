module stlc.Gensym

import stlc.Syntax

function
gensym : Exp -> Var

axiom
gensym(~e) == ~v
================== gensym-is-fresh
!isFreeVar(~v, ~e)


goal
~fresh == gensym(app(app(~e, ~e1), var(~x)))
============================================= fresh-unequal-var
~x != ~fresh

axiom
~fresh == gensym(app(app(~e, ~e1), var(~x)))
============================================= fresh-unequal-var
~x != ~fresh