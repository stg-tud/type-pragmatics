module sql.ProgressUnion

import sql.BasicFunctions
import sql.Syntax
import sql.Semantics
import sql.TypeSystem

axiom
~TTC |- ~sql1 : ~TT
~TTC |- ~sql2 : ~TT
~sql1 == TValue(~t1)
~sql2 == TValue(~t2)
================================= progress-union
exists ~t
	union(~t1, ~t2) == some(~t)