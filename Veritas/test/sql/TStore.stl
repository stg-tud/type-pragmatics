module sql.TStore

import sql.BasicFunctions
import sql.Tables


sorts
	TStore TName
	
constructors
	empty : TStore
	bind : TName Table TStore -> TStore
	
function
lookup : TName TStore -> Opt
lookup(n, empty) = none
lookup(n, bind(m, t, TS)) = 
	if (n == m)
	then some(t)
	else lookup(n, TS)
	
axioms
~x == ~y
bind(~x, ~Tx, bind(~y, ~Ty, ~C)) |- ~e : ~T
=========================================== T-TStore-Duplicate
bind(~x, ~Tx, ~C) |- ~e : ~T

~x != ~y
bind(~x, ~Tx, bind(~y, ~Ty, ~C)) |- ~e : ~T
=========================================== T-TStore-Swap
bind(~y, ~Ty, bind(~x, ~Tx, ~C)) |- ~e : ~T